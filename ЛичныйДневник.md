## Личный дневник разработки ClassQR (от моего лица)

Я веду этот дневник, чтобы фиксировать, **что я делал**, **с какими ошибками сталкивался** и **как мы их решали** при разработке/рефакторинге и деплое ClassQR.

Важно: я не храню здесь секреты (пароли/токены) в открытом виде — только описываю, что делал.

---

### 2026-04-27 — Самое начало: формулирование требований

Сегодня я сформулировал требования к системе **ClassQR** (опросы/занятия по QR + аналитика).

Что я хотел получить:
- **Сессии и QR**: преподаватель создаёт сессию/пару, система выдаёт QR для входа, студенты заходят по QR.
- **Вопросы**: выдача одного вопроса (или пула вопросов), рекомендации.
- **Ответы**: студенты отправляют ответы, преподаватель проверяет и ставит баллы/комментарии.
- **Рейтинг**: рейтинг по группе.
- **Кластеризация**: k-means (NumPy), силуэт, устойчивость и защита от вырожденных случаев (мало студентов, нулевая дисперсия и т.п.).

Ограничения, которые я сразу зафиксировал:
- Архитектуру **C4 не ломать** (контроллеры → сервисы → репозитории → модели).
- API для фронта **не ломать** (обратная совместимость маршрутов и форматов).
- Нужны релизные вещи: CORS для production, конфиги сред, Docker, healthcheck, идемпотентные миграции (SQLite/безопасные ALTER TABLE).

Формат результата, который я попросил:
- после каждого изменения объяснять, зачем оно нужно;
- в конце — список изменённых файлов, чек‑лист деплоя;
- при изменениях в архитектурной раскладке — диаграмма последовательности для сценария «студент отвечает на вопрос».

---

### 2026-04-27 — Рефакторинг и подготовка к MVP (backend/frontend)

#### Что мы сделали в коде
- Я проверил структуру проекта и точки входа API: backend оказался на Flask, маршруты регистрируются в `backend/app.py`.
- Мы добавили **централизованную обработку ошибок** (единый JSON `{"error": "..."}` + логирование 500), чтобы контроллеры не были забиты `try/except`.
- Мы добавили **healthcheck** `GET /health` с проверкой БД (это помогало при деплое и мониторинге).
- Мы сделали CORS **safe-by-default** для production и ввели `APP_ENV` + `SEED_DATA`.
- Мы ввели утилиты **валидации** и применили их к sessions/answers/questions, не ломая API.
- Мы оптимизировали **кластеризацию** (k-means + silhouette) и добавили защиту от вырожденных случаев.
- Мы убрали отладочный шум: заменили `print` на логирование там, где это было в рантайме.
- Для запуска на хостинге добавили `gunicorn`, а для фронта — поддержку `VITE_API_BASE`.

#### С какими ошибками я столкнулся и как мы решали
- Команда `python` в окружении не находилась → запускал проверки через `python3`.
- Тесты падали с `UNIQUE constraint failed: users.login`.
  - Исправили: test-режим `APP_ENV=test` отключает auto-create/seed; модульный `app = create_app()` не создаётся при `APP_ENV=test`; в фикстуре подчистили тестового пользователя.
  - Итог: `pytest` проходит.

---

### 2026-04-27 — Деплой на PythonAnywhere

#### Что я делал
- Я клонировал репозиторий, поднял виртуальное окружение и начал ставить зависимости.
- Настроил WSGI, чтобы backend работал как web‑app, и проверял работоспособность через `/health`.

#### С какими ошибками я столкнулся и как мы решали
- Ошибка `ModuleNotFoundError: No module named 'numpy'`.
  - Причина: я поставил зависимости из корневого `requirements.txt`, а `numpy` был в `backend/requirements.txt`.
  - Решение: поставить зависимости из `backend/requirements.txt`.
- Я открыл `/` и увидел 404 — сначала подумал, что “не работает”.
  - Оказалось нормально: backend — API, главной страницы `/` нет; проверка через `/health` показала `ok`.
- Я пытался собрать Vue-фронтенд прямо на PythonAnywhere.
  - `npm` не было; попытка поставить Node через `nodeenv` дала сломанный `npm` (ошибка про `are-we-there-yet`).
  - Решение: собирать `frontend/dist` локально и загружать как static (или вынести фронт на отдельный хостинг).

---

### 2026-04-27 — Деплой на Render (полностью браузерный вариант)

#### Backend (Web Service)
- Я поднял backend на Render и проверил его по `GET /health` (получил `status=ok`).

С какими ошибками я столкнулся:
- Render по умолчанию пытался собирать на Python 3.14, и падала установка `Pillow`.
  - Решение: перевёл backend на Docker (образ `python:3.11-slim`), после чего зависимости ставились нормально.
- Один раз ошибся с путями и получил `backend/backend: no such file or directory`.
  - Причина: одновременно выставил `Root Directory=backend` и `Dockerfile Path=backend/Dockerfile`.
  - Решение: не “удваивать” пути (либо root пустой + dockerfile `backend/Dockerfile`, либо root `backend` + dockerfile `Dockerfile`).

#### Frontend (Static Site)
- Я поднял фронтенд на Render как Static Site.

С какими ошибками я столкнулся:
- Сначала `dist` не создавался из‑за пустой build-команды (`Empty build command; skipping build` → `dist does not exist`).
  - Решение: выставить `npm ci && npm run build` и publish directory `dist`.

#### Связь фронт ↔ бэк
- В коде фронта API был на `/api` (это работало локально из-за Vite proxy).
- Для Render мы сделали переключение через `VITE_API_BASE`, чтобы фронт ходил на полный URL бэкенда.

---

### 2026-04-27 — Создание администратора без seed (bootstrap)

Проблема:
- При попытке логина я получал `401 Invalid credentials` и понял, что пользователей нет (в production seed выключен).

Решение:
- Мы добавили одноразовый механизм `POST /api/admin/bootstrap`, который:
  - включается только при наличии `BOOTSTRAP_TOKEN` в env,
  - требует заголовок `X-Bootstrap-Token`,
  - создаёт admin только если админа ещё нет.
- После создания администратора я должен **удалить `BOOTSTRAP_TOKEN`**, чтобы bootstrap снова стал недоступен (возвращал 404).

---

### 2026-04-27 — Как я сделал данные “непропадающими”

Проблема:
- Я заметил, что на хостинге могут “пропадать” пользователи/группы. Это похоже на использование SQLite в окружениях, где файловая система не гарантирует сохранность между перезапусками.

Решение:
- Я создал **Render PostgreSQL** (`classqr-db`) и переключил backend на неё через `DATABASE_URL`.
- Проверка: `/health` продолжил отвечать `db: ok` (значит подключение к Postgres успешное).

Следствие:
- База стала “постоянной”, но она новая и пустая → администратора и справочники нужно создать заново.

---

### 2026-04-27 — Правило: дневник дополняю после каждого запроса

Я зафиксировал правило: после каждого моего сообщения/шага я дополняю этот файл — что сделал, какую ошибку увидел, и как решили.

---

### Заметки на будущее (технический долг)

- Лучше использовать Postgres для production (я уже переключился на него).
- В проекте есть техдолг: две JWT-реализации (`middleware/auth_middleware.py` и `backend/utils/jwt_utils.py`) с разными payload-ключами (`id` vs `user_id`). Сейчас это не ломает поток, но требует внимания при развитии.

## Личный дневник разработки ClassQR (ClassQR / Dynamic clustering system of educational results)

Документ фиксирует, что было сделано в ходе подготовки проекта к MVP/релизу, и какие проблемы встречались при деплое (PythonAnywhere + Render), с решениями.

---

### 2026-04-27 — Старт: постановка требований и ограничений

#### Исходная задача (функциональные требования)
- Система **ClassQR**: опросы/занятия через **QR**, выдача вопросов, приём ответов, проверка/оценка, рейтинг и аналитика.
- Ключевые доменные модули:
  - **Сессии**: создание пары/сессии, генерация QR, вход студентов по QR.
  - **Вопросы**: выдача одного вопроса или пула вопросов, рекомендации.
  - **Ответы**: приём ответов, хранение, проверка, выставление баллов/комментариев.
  - **Рейтинг**: вычисление рейтинга по группе.
  - **Кластеризация**: k-means на NumPy, расчёт силуэта, динамика кластеров.

#### Что требовалось сделать по качеству/рефакторингу
- Пересмотреть бизнес‑логику всех модулей, убрать нелогичные/избыточные шаги и дубли.
- Упростить цепочки вызовов, привести имена к единому стилю (PEP 8 для Python; рекомендации Vue для фронта).
- Добавить валидацию входных данных на backend (типы, диапазоны, обязательные поля).
- Оптимизировать k-means: ускорение вычислений + защита от вырожденных случаев (мало студентов, нулевая дисперсия и т.п.).

#### Подготовка к MVP/релизу
- Удалить отладочный код (print/console.log/закомментированные блоки).
- Добавить централизованную обработку ошибок (понятные HTTP‑статусы, логирование).
- Настроить CORS под production (разрешить только домен фронта).
- Подготовить конфиги сред (development/production), `.env.example`.
- Docker/Docker Compose: добавить/оптимизировать; убедиться, что всё стартует предсказуемо.
- Проверить идемпотентность миграций БД (для текущей SQLite-схемы — безопасные `ALTER TABLE`).
- Добавить `GET /health` для мониторинга.

#### Ограничения
- **Не менять C4-архитектуру**: контроллеры → сервисы → репозитории → модели.
- **Не ломать существующий API** для уже работающего фронта (обратная совместимость маршрутов/форматов).

#### Формат результата
- Пояснять после каждого изменения: зачем сделано и чем улучшает систему.
- Итог: список изменённых файлов, краткое описание, чек‑лист готовности к деплою.
- При архитектурных правках — диаграмма последовательности для сценария «студент отвечает на вопрос».

---

### 2026-04-27 — Рефакторинг и подготовка к MVP

#### 1) Инвентаризация архитектуры и API
- **Backend**: Flask + Flask-SQLAlchemy, слои C4 сохранены: контроллеры → сервисы → репозитории → модели.
- **Точки входа API**: маршруты регистрируются в `backend/app.py` через `add_url_rule`, auth — blueprint `/api/auth`.
- **Ключевые сценарии**: создание сессий/QR, выдача вопросов, приём ответов, проверка/оценка, рейтинг, аналитика и кластеризация.

#### 2) Централизованная обработка ошибок + логирование
- Добавлен модуль `backend/utils/error_handlers.py` и подключение в `backend/app.py`.
- Введены обработчики `HTTPException`, `ValueError`, `PermissionError`, общий `Exception`.
- **Эффект**: единый JSON-формат ошибок `{"error": "..."}`, меньше дублирования `try/except`, скрытие внутренних 500 от клиента, логирование stack trace.

#### 3) Healthcheck для мониторинга
- Добавлен эндпоинт `GET /health` в `backend/app.py` с лёгкой проверкой БД (`SELECT 1`).
- **Эффект**: можно использовать в Docker/оркестрации и при деплое для быстрой проверки доступности.

#### 4) CORS и конфигурация окружений (dev/prod)
- В `backend/app.py` добавлена логика `APP_ENV`/`FLASK_ENV`.
- **Safe-by-default**:
  - `production`: CORS не разрешает ничего, пока явно не задан `CORS_ORIGINS`.
  - `development`: разрешены localhost + опционально LAN.
- `CORS_ALLOW_LAN` по умолчанию выключен в production.
- Seed-данные (`seed_data`) по умолчанию отключены в production через `SEED_DATA`.
- `.env.example` обновлён: секции common/dev/prod + пример домена `https://SenyaByy.pythonanywhere.com`.

#### 5) Валидация входных данных в контроллерах
- Добавлен `backend/utils/validation.py` (require_json/get_int/get_str/get_bool).
- Применено без изменения публичных маршрутов и ключей JSON:
  - `backend/controllers/session_controller.py`
  - `backend/controllers/answer_controller.py`
  - `backend/controllers/question_controller.py`
- **Эффект**: меньше 500 из-за невалидных payload, больше понятных 400, единый стиль.

#### 6) Оптимизация кластеризации (NumPy k-means) и защита от вырождения
- Файл: `backend/services/clustering_service.py`.
- Оптимизации:
  - расчёт расстояний без временного массива `(n,k,d)` через формулу \(||x-c||^2\).
  - пересчёт центроидов через агрегации `np.bincount`.
  - early stop по стабилизации меток.
  - guards: `k>=n`, NaN/inf, “все точки одинаковые” (быстрый выход).
  - silhouette: дистанции считаются 1 раз; для больших групп — детерминированный subsample.
  - ускорено извлечение признаков: убрано N+1 по Attendance, батч‑загрузка Questions.
- **Эффект**: быстрее и устойчивее на классах/группах, меньше нагрузки на БД.

#### 7) Удаление отладочного кода
- Убраны `print` в `backend/utils/jwt_utils.py`, заменены на `logging`.
- Дополнительно: в проекте обнаружен `print` только в `backend/seed_math_demo.py` (это демо-скрипт, не используется в рантайме API).

#### 8) Docker/Compose и релизная упаковка
- `backend/Dockerfile` приведён к порту **5001** (в соответствии с compose).
- В `docker-compose.yml` и `scripts/docker-compose.yml` добавлены:
  - `APP_ENV`, `SEED_DATA`, prod-ориентиры для `CORS_ORIGINS/QR_FRONTEND_ORIGIN`.
  - healthcheck для backend (`/health`) и nginx.
- Идемпотентные SQLite-миграции расширены в `_ensure_sqlite_migrations()` (добавлены guards для дополнительных колонок).

#### 9) Тесты/проверки
- Исправлен импорт и изоляция окружения в `backend/tests/test_auth.py`.
- Выявленная проблема: при автосидинге и особенностях инициализации приложения тесты ловили `UNIQUE constraint failed: users.login`.
- Решение:
  - тестовый режим `APP_ENV=test` отключает auto-create/seed внутри `create_app`.
  - модульный `app = create_app()` не создаётся при `APP_ENV=test` (чтобы не было побочных эффектов на import).
  - в фикстуре добавлена защитная очистка `testuser`.
- Итог: `pytest` проходит успешно (3 passed).

---

### 2026-04-27 — Деплой на PythonAnywhere (backend + подготовка frontend)

#### Проблема 1: нет `python` в PATH локально для тестов
- В локальной среде вызов `python` не работал, использовали `python3`.

#### Проблема 2: ModuleNotFoundError: numpy
- Причина: на PythonAnywhere зависимости ставились из корневого `requirements.txt`, в котором не было `numpy` (он был в `backend/requirements.txt`).
- Решение: `pip install -r backend/requirements.txt`.

#### Проблема 3: почему `/` отдаёт 404
- Причина: backend — это API, роут `/` не определён.
- Проверка правильности запуска: `GET /health` → `status=ok`.

#### Проблема 4: нет npm на PythonAnywhere для сборки Vue
- `npm: command not found`.
- Попытка поставить Node через `nodeenv` привела к сломанному npm (`Cannot find module 'are-we-there-yet'`).
- Рекомендованный обход: собирать `frontend/dist` локально и загружать как static.

#### Настройка WSGI
- Добавлен `backend/wsgi.py` (переменная `application` для PythonAnywhere/WSGI).
- Добавлена инструкция `docs/DEPLOY_PYTHONANYWHERE.md`.

---

### 2026-04-27 — Деплой на Render (полностью браузерный вариант)

#### Проблема 1: Render использовал Python 3.14 по умолчанию, падала сборка Pillow
- Симптом: `Using Python version 3.14.3 (default)` и падение сборки `Pillow==10.2.0` (не было wheel).
- Попытка: добавить `runtime.txt` (в root и `backend/`) с `python-3.11.9`.
- Итог: Render всё равно тянул 3.14 → переход на Docker.

#### Решение: Backend как Docker Web Service
- Использован `backend/Dockerfile` на `python:3.11-slim` (уходит проблема 3.14).
- Важно было не “удвоить путь”:
  - ошибка `backend/backend` возникала при одновременном `Root Directory=backend` и `Dockerfile Path=backend/Dockerfile`.
  - исправление: либо Root Directory пусто + Dockerfile Path `backend/Dockerfile`, либо Root Directory `backend` + Dockerfile Path `Dockerfile`.
- Проверка: `GET /health` на Render → `ok`.

#### Frontend на Render Static Site
- Важная ошибка: `Empty build command; skipping build` → `dist does not exist`.
- Решение: задать build команду `npm ci && npm run build`, publish directory `dist`, root directory `frontend`.

#### Связь frontend ↔ backend (API baseURL)
- По коду фронт ходил на `/api` (dev proxy решал локально).
- Для Render добавлена поддержка `VITE_API_BASE`:
  - `frontend/src/api/index.js`: baseURL берётся из `import.meta.env.VITE_API_BASE` или fallback `/api`.
- Для backend добавлен `gunicorn` в `backend/requirements.txt` (для корректного запуска на хостинге).

#### Проблема: 401 Invalid credentials
- Это означало, что связь фронт↔бэк есть, но нет пользователя в БД.
- В production seed выключен (`SEED_DATA=false`), поэтому дефолтные пользователи не создаются.

#### Bootstrap администратора без seed (инициализация первого admin)
- Потребность: создать первого admin без shell.
- Реализовано:
  - `POST /api/admin/bootstrap` (работает только если задан env `BOOTSTRAP_TOKEN` и при наличии заголовка `X-Bootstrap-Token`).
  - Создаёт admin только если админа ещё нет.
- Рекомендация: после создания администратора удалить `BOOTSTRAP_TOKEN` из env и перезапустить сервис (endpoint станет 404).

#### Проблема: данные (пользователи/группы) могут «пропадать»
- Причина: при использовании SQLite на Render (особенно на free) данные могут теряться при redeploy/усыплении/перезапуске сервиса.
- Решение: перейти на Render PostgreSQL.
  - Создана БД `classqr-db` (Free, Oregon).
  - В backend service переменная `DATABASE_URL` переключена на **Internal Database URL** Postgres.
  - Проверка: `GET /health` продолжает возвращать `{"status":"ok","db":"ok","env":"production"}` — значит подключение успешно.
- Следствие: БД новая и пустая → нужно заново создать admin (bootstrap) и перелогиниться во фронте.

---

### 2026-04-27 — Организационные договорённости по документации

#### Требование: дополнять дневник после каждого запроса
- Пользователь попросил: **«не прекращай заполнять/дополнять личный дневник после каждого запроса»**.
- Договорённость: после каждого сообщения фиксировать в `ЛичныйДневник.md`:
  - что было сделано/проверено,
  - какие ошибки/риски выявлены,
  - какое решение принято и какие следующие шаги.

#### Bootstrap token для создания администратора (Render Postgres)
- Пользователь передал значение `BOOTSTRAP_TOKEN` для разовой инициализации администратора в новой Postgres-БД.
- Следующие шаги:
  - временно включить `BOOTSTRAP_TOKEN` в env backend-сервиса,
  - выполнить `POST /api/admin/bootstrap` с заголовком `X-Bootstrap-Token`,
  - после успеха удалить `BOOTSTRAP_TOKEN` и перезапустить backend.

---

### Нерешённые/важные замечания (технический долг)
- На Render **SQLite** может быть непредсказуем (файловая система не гарантирует сохранность на free). Для реального прод лучше Postgres.
- В проекте есть **дублирование JWT утилит** (`middleware/auth_middleware.py` и `backend/utils/jwt_utils.py`) с разными payload-ключами (`id` vs `user_id`). Сейчас это не ломает поток, но опасно при дальнейшем расширении.

---

### 2026-04-27 — Исправление 404 при обновлении + документация + уборка репозитория

- Исправил проблему **404 при обновлении страниц** на Render Static Site (Vue Router history): добавил правило **Redirects/Rewrites** `/* → /index.html` (Rewrite).
- Обновил/добавил документацию:
  - `docs/DEPLOY_RENDER.md` (деплой на Render + обязательный rewrite для SPA)
  - `docs/TESTING_MANUAL_STATIC.md` (статическое и ручное динамическое тестирование; подставлен frontend `classqr-frontend.onrender.com`)
  - привёл `README.md` и архитектуру к актуальному состоянию (Render + SPA rewrite).
- Почистил репозиторий от мусора/дублей:
  - убрал локальные артефакты (`.DS_Store`, `.idea/`, `.pytest_cache/`, `.venv/`, `venv/`)
  - удалил неактуальные/дублирующие файлы (`backend/README.md`-пустышка, корневые `requirements.txt`/`runtime.txt`, дубли в `scripts/`)
  - перенёс “канонические” документы в `docs/` и удалил дубликаты из корня (план/архитектура).
- Начал подготовку к автотестам: в проекте уже есть `backend/tests/test_auth.py`, следующий шаг — добавить pytest-smoke для сценария QR→ответ→проверка.

---

### 2026-04-27 — Автотесты: расширение покрытия основного функционала

- Добавил/расширил автотесты `pytest` для основного сценария (teacher→session→live-qr→verify-ticket→submit→grade) и негативных кейсов:
  - запрет повторной отправки ответа
  - базовые проверки ролей (student не может grade/создавать сессию)
- Обновил документы по тестированию:
  - `docs/TESTING_MANUAL_STATIC.md` (как запускать автотесты)
  - `docs/REPORT_DYNAMIC_TESTING.md` (зафиксирован результат прогона: **8 passed**)

